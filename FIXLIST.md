# WordSnap 优化修复清单

> **规则**：本文件是本轮优化的执行契约。每一项先写验收标准，开工后对照实现，
> 完成即在条目标记 `[x]` 并附 commit 哈希。**全部勾选前不结束本轮工作。**
> 顺序原则：P1 → T-1 → P2 → P3 → Q（T-1 提前，因为 P2-2 撤销依赖词笔记的
> 创建标记；每项独立提交）。

## P1 第一批 · 动线与情绪（小而直接）

- [x] **P1-1 剪贴板预填**：⌘L 呼出时剪贴板若为单个英文词则预填输入框并全选；
  同一个词不连续重复预填（记忆上次预填词）；**不清空用户剪贴板**（预览阶段
  设想的「查完清剪贴板」有破坏性，废弃该设计）。
  验收：复制单词 → ⌘L → 直接回车即可查。
- [x] **P1-2 发音按钮**：结果卡单词旁新增 🔊，点击播放有道 dictvoice 美音
  （AVPlayer 流式播放，独立小类不阻塞界面）。验收：点击出声、连点不叠音。
- [x] **P1-3 存词计数与连续天数**：保存成功横幅显示「第 N 个词 · 连续 M 天」；
  数据从表格文件统计，统计函数 `stats(in:on:)` 可单测（连续天数按日期列）。
- [x] **P1-4 去重→重逢**：重复保存不再走红色错误样式，改为中性温暖的
  「已在 <日期> 存过，又遇到它了」+ 时钟图标（新增 `.reunion` 状态，
  从已存在行取原保存日期）。

## T 模板 · 词笔记（Obsidian 管理端）

- [x] **T-1 NoteWriter**：保存/重逢时在表格同目录 `Vocabulary/` 下维护词笔记：
  - 模板：frontmatter（word / phonetic / pos / added / source / star /
    status / tags）+ 正文（释义 / 英文释义 / 例句 / 我的痕迹）+
    末尾 `word::释义` 单行卡（兼容 obsidian-spaced-repetition）；
  - 柯林斯星级从接口解码入 frontmatter（`★★★★☆` 形式）；
  - 已存在则不覆盖；「重逢」时在「我的痕迹」追加一行且只追加；
  - 新建/追加/幂等逻辑可单测（临时目录注入）。
  表格写入逻辑保持不变——表格是日志，笔记是词的家。

## P2 第二批 · 容错与掌控

- [x] **P2-1 suggest 纠错**：notFound 时调有道 suggest 接口，错误态下方列出
  ≤4 个候选词胶囊，点击直接重查；解析函数可单测；接口失败静默跳过。
- [x] **P2-2 撤销窗口**：保存成功横幅停留延长到 4s 并提供「撤销」：移除刚
  写入的表行；若词笔记是本次新建的则一并删除（原本就存在的笔记不动）；
  自动隐藏计时器可被撤销取消。
- [x] **P2-3 可配置热键**：菜单栏新增「设置…」窗口：
  - 快捷键录制（本地事件监听，Esc 取消，须含 Cmd/Ctrl/Opt 修饰键），
    持久化 UserDefaults，变更即时重注册全局热键；
  - Obsidian 路径选择器（NSOpenPanel，写 `~/.wordsnap.json`，即时生效）。

## P3 第三批 · 长期陪伴

- [x] **P3-1 今日一词**：从词表按日期种子确定性随机取一个旧词（可单测），
  菜单栏首项展示；点击呼出面板并自动查询；面板空态显示幽灵提示。
- [x] **P3-2 热力图**：菜单栏内嵌近 16 周 GitHub 风格打卡格
  （`dateCounts(in:)` 可单测），四级绿色。

## Q 收尾

- [ ] **Q-1 全量验证**：`xcodebuild build + test` 全绿（新增 stats /
  suggest-parser / NoteWriter / wordOfTheDay / dateCounts 用例，原有用例
  同步适配）；README 与 SPEC 更新新能力；最终构建并重启应用。
